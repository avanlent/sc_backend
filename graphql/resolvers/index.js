const generic = require('./generic.resolvers');
const setResolver = require('./set.resolvers');
const cardResolver = require('./card.resolvers');

module.exports = { generic, setResolver, cardResolver };